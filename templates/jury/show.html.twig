{% extends 'base.html.twig' %}

{% block title %}Jury{% endblock %}

{% block body %}
    <h1>Jury</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ jury.id }}</td>
        </tr>
        <tr>
            <th>Date</th>
            <td>{{ jury.date }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('app_jury_index') }}">back to list</a>
    {% if userRole != 'Enseignant'  %}
    <a href="{{ path('app_jury_edit', {'id': jury.id}) }}">edit</a>
    {% endif %}

    {{ include('jury/_delete_form.html.twig') }}
{% endblock %}